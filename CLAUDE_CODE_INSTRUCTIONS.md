# Server Health Telegram Bot — Инструкции для Claude Code

## Цель проекта

Создать Telegram-бота для мониторинга и управления здоровьем Linux-серверов:
- Регулярные отчёты о состоянии серверов
- Команды для ручного запуска проверок
- Возможность применять оптимизации через бота
- Поддержка нескольких серверов

---

## Архитектура

```
Telegram Bot (Python + aiogram 3.x)
    ├── SSH Manager (asyncssh) — подключение к серверам
    ├── Health Checker — сбор метрик и формирование отчётов
    ├── Scheduler (APScheduler) — регулярные проверки
    └── SQLite — конфигурация серверов
```

---

## Технологический стек

- **Python 3.11+**
- **aiogram 3.x** — Telegram Bot API
- **asyncssh** — асинхронные SSH-подключения
- **APScheduler** — планировщик задач
- **SQLite** — хранение конфигурации серверов
- **python-dotenv** — переменные окружения

---

## Структура проекта

```
server-health-bot/
├── .env                    # Секреты (BOT_TOKEN, ADMIN_ID)
├── .env.example            # Пример .env
├── requirements.txt        # Зависимости
├── config.py               # Конфигурация
├── main.py                 # Точка входа
├── bot/
│   ├── __init__.py
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── commands.py     # Обработчики команд
│   │   ├── callbacks.py    # Callback-кнопки
│   │   └── admin.py        # Админские команды
│   ├── keyboards/
│   │   ├── __init__.py
│   │   └── inline.py       # Inline-клавиатуры
│   └── middlewares/
│       ├── __init__.py
│       └── auth.py         # Проверка доступа
├── core/
│   ├── __init__.py
│   ├── ssh_manager.py      # SSH-подключения
│   ├── health_checker.py   # Сбор метрик
│   ├── report_formatter.py # Форматирование отчётов
│   └── optimizer.py        # Команды оптимизации
├── database/
│   ├── __init__.py
│   ├── models.py           # Модели данных
│   └── crud.py             # CRUD операции
├── scheduler/
│   ├── __init__.py
│   └── jobs.py             # Задачи по расписанию
└── scripts/
    └── health_check.sh     # Скрипт сбора метрик
```

---

## Команды бота

### Основные команды
```
/start              — Приветствие и помощь
/help               — Список команд
/servers            — Список серверов
/status             — Быстрый статус всех серверов
/check <server>     — Полная проверка сервера
/check_all          — Проверить все серверы
```

### Управление серверами
```
/add_server         — Добавить новый сервер
/remove_server      — Удалить сервер
/edit_server        — Изменить настройки сервера
```

### Оптимизация (с подтверждением)
```
/optimize <server>  — Показать рекомендации
/clear_logs <srv>   — Очистить старые логи
/clear_cache <srv>  — Очистить кэш
/restart <service>  — Перезапустить сервис
```

### Настройки
```
/schedule           — Настройка расписания отчётов
/alerts             — Настройка алертов
/settings           — Общие настройки
```

---

## Формат отчёта в Telegram

```
🖥 Server: production-1
📅 2024-01-15 10:30 UTC
⏱ Uptime: 45 days

📊 Общее состояние: 🟡 Требует внимания

💻 Ресурсы:
├ CPU:  ██████░░░░ 62% 🟢
├ RAM:  ████████░░ 78% 🟡
├ Swap: ██░░░░░░░░ 15% 🟢
├ Disk: █████████░ 87% 🔴
└ Load: 2.4 / 4 cores

⚠️ Проблемы:
• Диск / заполнен на 87%
• RAM выше 75%

💡 Рекомендации:
• Очистить логи (освободит ~2.3GB)
• Удалить старые ядра

[🔄 Обновить] [🛠 Оптимизировать] [📋 Подробнее]
```

---

## Этапы разработки

### Этап 1: Базовый бот (текущий)
1. Настроить окружение и зависимости
2. Создать структуру проекта
3. Реализовать SSH-подключение к текущему серверу
4. Сбор базовых метрик (CPU, RAM, Disk)
5. Форматирование и отправка отчёта в Telegram
6. Команда /check для ручной проверки

### Этап 2: Планировщик
1. Добавить APScheduler
2. Регулярные проверки по cron
3. Команда /schedule для настройки

### Этап 3: База данных серверов
1. SQLite для хранения серверов
2. CRUD операции
3. Команды /add_server, /remove_server
4. Поддержка нескольких серверов

### Этап 4: Оптимизация
1. Анализ проблем
2. Безопасные команды оптимизации
3. Подтверждение перед выполнением
4. Логирование действий

### Этап 5: Алерты
1. Пороговые значения
2. Мгновенные уведомления при проблемах
3. Настройка через бота

---

## Важные правила

### Безопасность
- SSH-ключи хранятся только на сервере с ботом
- Доступ к боту только для ADMIN_ID
- Опасные команды требуют подтверждения
- Все действия логируются

### Обработка ошибок
- Таймауты для SSH (30 сек)
- Graceful handling при недоступности сервера
- Уведомление админа об ошибках

### Производительность
- Асинхронные SSH-подключения
- Параллельная проверка серверов
- Кэширование результатов (5 мин)

---

## Переменные окружения (.env)

```env
# Telegram
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id

# Database
DATABASE_PATH=./data/servers.db

# SSH defaults
SSH_TIMEOUT=30
SSH_KEY_PATH=~/.ssh/id_rsa

# Scheduler
CHECK_INTERVAL_HOURS=6
ALERT_CHECK_INTERVAL_MINUTES=15

# Thresholds
CPU_WARNING=70
CPU_CRITICAL=90
RAM_WARNING=70
RAM_CRITICAL=85
DISK_WARNING=70
DISK_CRITICAL=85
```

---

## Начало работы

### Шаг 1: Создать бота в Telegram
1. Открыть @BotFather
2. /newbot
3. Сохранить токен

### Шаг 2: Узнать свой Telegram ID
1. Открыть @userinfobot
2. Сохранить ID

### Шаг 3: Настроить SSH-ключ
```bash
# Если ключа нет — создать
ssh-keygen -t ed25519 -C "health-bot"

# Для локального сервера ключ уже настроен (мы подключены по SSH)
```

### Шаг 4: Установить зависимости
```bash
pip install -r requirements.txt
```

### Шаг 5: Настроить .env
```bash
cp .env.example .env
# Заполнить BOT_TOKEN и ADMIN_ID
```

### Шаг 6: Запустить
```bash
python main.py
```

---

## Команды для тестирования

```bash
# Тест SSH локально
ssh localhost "uptime && free -h && df -h /"

# Тест бота
python -c "from bot import test; test()"
```

---

## Следующие шаги после базовой версии

1. **Добавить второй сервер** — через /add_server
2. **Настроить systemd** — автозапуск бота
3. **Docker** — контейнеризация (опционально)
4. **Grafana** — визуализация метрик (опционально)
